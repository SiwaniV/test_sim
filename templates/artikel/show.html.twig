{% extends 'base.html.twig' %}

{% block title %}Artikel{% endblock %}

{% block body %}
    <h1>Artikel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artikel.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ artikel.Naam }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('artikel_index') }}">Terug naar de lijst</a>

    <a class="btn btn-dark" href="{{ path('artikel_edit', {'id': artikel.id}) }}">Bewerk</a>

    {{ include('artikel/_delete_form.html.twig') }}
{% endblock %}
